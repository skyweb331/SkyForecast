var chai = require('chai'),
    chaiHttp = require('chai-http'),
    server = require('../server/server.js'),
    should = chai.should();

chai.use(chaiHttp);

describe('Node Test', function(){
    it('should list all blobs on');
});